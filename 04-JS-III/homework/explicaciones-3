	* Arrays: Es una estructura de datos similar a una lista, que puede contener una cierta cantidad de elementos, desde strings y numeros, hasta variables y funciones. Las 
        listas tienen indices numerados mediante los cuales se puede acceder a cada elemento por separado.